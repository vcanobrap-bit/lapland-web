/**
 * Placeholder. En el commit 4 esta página pasa a traer el global `Home`
 * desde Payload y delegar el render en `<RenderBlocks />`.
 */
export default function HomePage() {
  return (
    <main className="mx-auto flex min-h-dvh max-w-2xl flex-col justify-center px-6">
      <p className="text-muted text-sm tracking-wide uppercase">Lapland</p>
      <h1 className="text-ink mt-3 text-4xl font-medium tracking-tight text-balance">
        Base lista. Payload entra en el próximo paso.
      </h1>
    </main>
  )
}
